# Studio Benchmark API

Run:

```bash
python3 examples/studio_benchmark_api_quantumbridge.py
```

The example runs the Stage 10B `circuit_basic` benchmark suite through the
Studio benchmark service. Output is not official Benchpress output and is not
production performance ranking.
