# QOS-UQCI Mock Runtime With QuantumBridge

Run:

```bash
python3 examples/qos_uqci_mock_runtime_quantumbridge.py
```

The example executes the QOS-UQCI job spec through a QuantumBridge offline mock
runtime and prints result JSON, counts, warnings, and provenance.
