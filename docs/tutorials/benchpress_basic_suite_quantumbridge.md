# Benchpress Basic Suite With QuantumBridge

Run:

```bash
python3 examples/benchpress_basic_suite_quantumbridge.py
```

The example executes local basic circuit benchmarks and prints a serialized
QuantumBridge benchmark result plus a Markdown report. It is not official
Benchpress output and does not access cloud services, tokens, or hardware.
