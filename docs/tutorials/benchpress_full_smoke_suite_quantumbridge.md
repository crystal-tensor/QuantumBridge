# Benchpress Full Smoke Suite With QuantumBridge

Run:

```bash
python3 examples/benchpress_full_smoke_suite_quantumbridge.py
```

The example executes all Stage 10B default local benchmark suites and produces
JSON / Markdown report content. It is a deterministic educational smoke suite,
not official benchmark output and not production performance ranking.
