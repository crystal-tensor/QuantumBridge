# Quafu Mock Backend With QuantumBridge

Run:

```bash
python3 examples/quafu_mock_backend_quantumbridge.py
```

The example executes a Quafu-compatible payload through a QuantumBridge offline
mock backend and prints result JSON, counts, warnings, and provenance.
